using Microsoft.AspNetCore.Identity;

namespace SalesManagementSystem.Models
{
    public class AppUser : IdentityUser
    {
        // This should contain the employee and client information, but for now I will keep it as is.
    }
}
